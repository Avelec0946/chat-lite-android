{
  "name": "chat-lite-android",
  "version": "1.0.0",
  "description": "chat-lite Android APK 版本（Capacitor 打包）",
  "main": "index.js",
  "scripts": {
    "sync:web": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:debug": "npx cap sync android && cd android && .\\gradlew assembleDebug",
    "build:release": "npx cap sync android && cd android && .\\gradlew assembleRelease"
  },
  "keywords": [
    "chat-lite",
    "android",
    "capacitor"
  ],
  "author": "Avelec0946",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.1",
    "@capacitor/haptics": "^6.0.3",
    "@capacitor/share": "^6.0.2",
    "capacitor-rich-haptics": "^0.10.4",
    "capacitor-stream-http": "^0.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
