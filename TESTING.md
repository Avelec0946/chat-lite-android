# chat-lite APK 构建完成 · 真机测试清单

> 构建时间：2026-07-19 18:24
> APK 路径：`C:\Users\86176\chat-lite-android\chat-lite-debug.apk`（3.7 MB）
> 基于主仓库 commit：`7c651bf`

---

## 一、安装到手机

### 方式 A：adb 安装（推荐，手机开启 USB 调试）

```powershell
adb install -r "C:\Users\86176\chat-lite-android\chat-lite-debug.apk"
```

### 方式 B：手动拷贝

把 `chat-lite-debug.apk` 拷到手机，文件管理器点击安装（需要开启"允许未知来源"）。

---

## 二、首次启动配置

1. 打开 chat-lite
2. 进入「设置」→「接口管理」→ 添加接口
3. 填入你的 Provider 信息（如 kkaiapi / DeepSeek）
4. 保存，回到主界面发送一条消息测试

---

## 三、功能测试清单

### 核心功能（必测）

| # | 测试项 | 期望结果 | 通过 |
|---|--------|---------|------|
| 1 | 应用启动 | 正常打开，显示对话列表（首次为空） | ☐ |
| 2 | 添加 Provider | 设置里能添加并保存接口 | ☐ |
| 3 | 发送消息 | 能调用 API，返回内容（**注意：是非流式，等几秒后逐字显示**） | ☐ |
| 4 | 思考链显示 | 开启深度思考时，回复上方显示思考过程 | ☐ |
| 5 | 停止生成 | 生成过程中点停止按钮，UI 立即停止打字 | ☐ |
| 6 | 长按删除消息 | 长按消息弹出菜单，删除后切换到兄弟节点 | ☐ |
| 7 | 分支树 | 点分支总览按钮，能看到节点树 | ☐ |
| 8 | 切换对话 | 侧栏切换对话，消息正确加载 | ☐ |

### 数据迁移测试（重要）

| # | 测试项 | 期望结果 | 通过 |
|---|--------|---------|------|
| 9 | 网页版导出全部 | 在网页版设置里点「导出全部数据」，得到 JSON | ☐ |
| 10 | APK 导入全部-覆盖 | APK 设置里选「覆盖」模式导入，数据完整恢复 | ☐ |
| 11 | APK 导出全部 | APK 设置里点「导出全部数据」，弹出分享框，能保存 | ☐ |
| 12 | 导入冲突测试 | 已有数据时导入新数据，「合并」模式按 ID 去重 | ☐ |

### 体验测试

| # | 测试项 | 期望结果 | 通过 |
|---|--------|---------|------|
| 13 | 按钮触摸区域 | 所有按钮至少 44px 高，不易误触 | ☐ |
| 14 | 后台切前台 | 切到后台再切回，数据和对话状态不丢 | ☐ |
| 15 | 离线打开 | 无网络时能打开应用，发送消息时提示错误 | ☐ |
| 16 | 直连模式开关 | APK 里设置面板**不显示**直连模式开关（已隐藏） | ☐ |

### 边界测试

| # | 测试项 | 期望结果 | 通过 |
|---|--------|---------|------|
| 17 | 长回答 | 让模型输出长内容（>2000 字），打字动画顺畅 | ☐ |
| 18 | 请求超时 | 配置一个不可达的 endpoint，120 秒后提示超时 | ☐ |
| 19 | 错误响应 | 故意填错 API Key，提示错误信息 | ☐ |
| 20 | 继续生成 | 中断的消息点「继续生成」，能续上 | ☐ |

---

## 四、已知限制（非 bug）

1. **流式是假动画**：响应完整返回后才逐字显示，长回答会有几秒等待。这是 P0 方案，后续可升级真流式（方案 B）。
2. **停止生成不能真正中断**：点停止后 UI 立即停，但底层 HTTP 请求会继续到返回，然后被丢弃。
3. **CDN 依赖**：highlight.js / marked.min.js 从 CDN 加载，离线时语法高亮和 Markdown 渲染会失败（但聊天功能不受影响）。
4. **API Key 存本地**：Key 存在 WebView 的 IndexedDB 里，不要把 debug APK 分给别人。

---

## 五、如果出问题

### 应用打不开 / 闪退

```powershell
# 查看日志
adb logcat | grep -i "chatlite\|capacitor"
```

### 发不出消息

1. 检查 Provider 配置是否正确（API 地址、Key、模型名）
2. 检查网络
3. 看应用内是否有错误提示
4. 用 adb logcat 看 console 输出

### 数据导入失败

1. 检查 JSON 文件是否完整
2. 检查文件格式是否是 chat-lite 导出的
3. APK 模式下导入前会自动备份到 Cache，不会丢当前数据

### 反馈给我

把以下信息发给我：
- 哪个测试项失败
- 错误现象（截图 / 文字描述）
- adb logcat 的相关输出

---

## 六、构建产物位置

| 文件 | 路径 |
|------|------|
| Debug APK（项目根） | `C:\Users\86176\chat-lite-android\chat-lite-debug.apk` |
| Debug APK（原始） | `C:\Users\86176\chat-lite-android\android\app\build\outputs\apk\debug\app-debug.apk` |
| 项目源码 | `C:\Users\86176\chat-lite-android\` |

---

## 七、后续可选优化（P1/P2）

- [ ] 升级真流式（方案 B）：WebView fetch + AbortController
- [ ] 应用图标（目前是 Capacitor 默认图标）
- [ ] 签名 release 版本
- [ ] 主仓库更新同步流程实测

完成测试后告诉我结果，有问题我来修。
